# Netlify deploy config — alternative to Vercel
[build]
  base    = "frontend"
  command = "yarn install && yarn build"
  publish = "frontend/build"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
